# Dependencies
node_modules/
app/node_modules/

# Build outputs
dist/
app/dist/
build/
app/build/

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
app/.env*

# Editor folders
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Project specific exclusions
out.txt
parsed.txt
~$*
*.xls
*.xlsx
*.pdf
# I'll keep the logo.png as it's part of the app assets if it's referenced. 
# But the user has several PDFs and XLs in the root which seem like raw data.
